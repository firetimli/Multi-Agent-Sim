# Version 0 -- deliberately minimal.
#
# The internal CodeArtifact mirror may serve sdists only; if pip tries to build
# numpy from source, install with:
#     pip install --only-binary=:all: -r requirements.txt
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
lightgbm>=3.3        # 4.x preferred; 3.3 works
PyYAML>=6.0
